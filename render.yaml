services:
  - type: web
    name: leukai-classifier
    env: static
    staticPublishPath: .
    buildCommand: echo "No build needed - static site"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
